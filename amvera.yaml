meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"

build:
  requirementsPath: requirements.txt

run:
  command: python web_app.py
  containerPort: 8000
  persistenceMount: /data
